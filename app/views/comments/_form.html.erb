<%= form_for([@article, @article.comments.build]) do |f| %>
    <p>
        <%= f.label :commentor %> <br>
        <%= f.text_field :commentor %>
    </p>
    <p>
        <%= f.label :text %> <br>
        <%= f.text_area :text %> 
    </p>
    <p>
        <%= f.submit %>
    </p>
<% end %>